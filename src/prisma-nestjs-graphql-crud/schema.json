{
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "default": "example-cart",
      "x-prompt": "What is your domain name?"
    },
    "path": {
      "type": "string",
      "minLength": 1,
      "default": "src/modules",
      "x-prompt": "where do you want to create the module?"
    }
  },
  "required": [
    "name"
  ]
}